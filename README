python -m venv venv
source venv/bin/activate

# pip install python-chess
# homebrew install stockfish for evaluations (? not for this project)

# pip install sklearn

# jupyter-lab

# I want to make a drop columns function

# gotta drop pgn

# cd to frontend and then run vercel --prod # to deploy to production chesslyzer.vercel.app

#to run this locally, just python the flask server (python generate.py)

#data_processing.py, visualization.py, analytics.py
#next step to split my chesslytics.py into those 3 to make stat extraction easier

# pip install -r requirements.txt

# pip-compile requirements.in